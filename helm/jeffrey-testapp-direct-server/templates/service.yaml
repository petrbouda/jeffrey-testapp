apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeffrey-testapp-direct-server.fullname" . }}
  labels:
    {{- include "jeffrey-testapp-direct-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jeffrey-testapp-direct-server.selectorLabels" . | nindent 4 }}
