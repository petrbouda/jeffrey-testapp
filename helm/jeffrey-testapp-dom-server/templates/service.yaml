apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeffrey-testapp-dom-server.fullname" . }}
  labels:
    {{- include "jeffrey-testapp-dom-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jeffrey-testapp-dom-server.selectorLabels" . | nindent 4 }}
