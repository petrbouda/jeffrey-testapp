apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jeffrey-testapp-client.fullname" . }}-app-config
  labels:
    {{- include "jeffrey-testapp-client.labels" . | nindent 4 }}
data:
  application.properties: |-
{{ .Values.applicationProperties | indent 4 }}
