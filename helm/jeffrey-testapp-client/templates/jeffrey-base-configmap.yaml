apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jeffrey-testapp-client.fullname" . }}-jeffrey-base
  labels:
    {{- include "jeffrey-testapp-client.labels" . | nindent 4 }}
data:
  jeffrey-base.conf: |-
{{ .Files.Get "jeffrey-base.conf" | indent 4 }}
