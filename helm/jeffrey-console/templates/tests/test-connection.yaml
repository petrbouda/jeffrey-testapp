apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jeffrey-console.fullname" . }}-test-connection"
  labels:
    {{- include "jeffrey-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jeffrey-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
